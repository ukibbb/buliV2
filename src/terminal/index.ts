/** Public terminal runtime API used by application composition and screens. */
export { openExternalUrl } from "@/terminal/open-external-url"
export { TerminalViewport } from "@/terminal/TerminalViewport"
export { Lifetime } from "@/terminal/renderer/lifetime"
export { runTuiRenderer } from "@/terminal/renderer/run-tui-renderer"
