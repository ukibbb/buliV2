/** Public authentication presentation API used by application screens. */
export { AuthenticationFlow } from "@/authentication/ui/AuthenticationFlow"
export type {
    TAuthenticationMode,
    TAuthenticationOutcome,
} from "@/authentication/ui/types"
